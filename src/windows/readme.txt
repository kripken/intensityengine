xxxYou should extract into this directory the contents of the 'Windows dev pack', a separate binary download. It contains includes and library files for SDL, Boost, etc., that are required to build on Windows.

